storePaperSize
setCollectLaying !!! lay ผิดเกรน alertNumLay

2.3 O-V: V
1. w_size = cal by 1
2. l_size = cal by 1
3. w_size + component <= paperW ? 
    wNum + 1 
    w_size = cal by wNum
    w_size + component > paperW &&
        wNum - 1
        w_size = cal by wNum
        break
4. do same as 3. but use l_size compare paperL
5. wNum == 1 ?
        lCase = 2.0
        l_size = cal by 1
        lNum = paperL / l_size
        l_size * lNum + component > paperL && 
            lNum - 1
        printL = l_size * lNum
    : printL = l_size
6. printW = W_size
--------
2.4 O-V: L
1. w_size = cal by 1
2. l_size = cal by 1
3. w_size + component <= paperL ? 
    wNum + 1 
    w_size = cal by wNum
    w_size + component > paperL &&
        wNum - 1
        w_size = cal by wNum
        break
4. do same as 3. but use l_size compare paperW
5. wNum == 1 ?
        lCase = 2.0
        l_size = cal by 1
        lNum = paperW / l_size
        l_size * lNum + component > paperW && 
            lNum - 1
        printW = l_size * lNum
    : printW = l_size
ุ6. printL = w_size
----- //////////// ---------


3.1 Straight / vertical
1. w_size = cal by 1
2. l_size = cal by 1
3. lNum = paperL / l_size
4. w_size + comp. <= paperW && 
    wNum + 1 
    w_size = cal by wNum
    w_size + comp. > paperW &&
        wNum - 1
        w_size = cal by wNum
        break;
5. l_size * lNum + comp. > paperL && 
    lNum - 1
6.printW = w_size
7.printL = l_size * lNum



----------

3.2 Straight / horizontal
1. w_size = cal by 1
2. l_size = cal by 1
3. wNum = paperW / l_size
4. w_size + comp. <= paperL && 
    lNum + 1 
    w_size = cal by lNum
    w_size + comp. > paperL &&
        lNum - 1
        w_size = cal by lNum
        break;
5. l_size * wNum + comp. > paperW && 
    wNum - 1
6.printL = w_size
7.printW = l_size * wNum


-------
3.3 O-V: V
1. w_size = cal by 1
2. l_size = cal by 1
3. w_size + component <= paperW ? 
    wNum + 1 
    w_size = cal by wNum
    w_size + component > paperW &&
        wNum - 1
        w_size = cal by wNum
        break
4. do same as 3. but use l_size compare paperL
5. wNum == 1 ?
        lCase = 3.0
        l_size = cal by 1
        lNum = paperL / l_size
        l_size * lNum + component > paperL && 
            lNum - 1
        printL = l_size * lNum
    : printL = l_size
6. printW = W_size
-------
3.3 O-V: H
1. w_size = cal by 1
2. l_size = cal by 1
3. w_size + component <= paperL ? 
    lNum + 1 
    w_size = cal by lNum
    w_size + component > paperL &&
        lNum - 1
        w_size = cal by lNum
        break
4. do same as 3. but use l_size compare paperW
5. lNum == 1 ?
        lCase = 3.0
        l_size = cal by 1
        wNum = paperW / l_size
        l_size * wNum + component > paperW && 
            wNum - 1
        printW = l_size * wNum
    : printW = l_size
6. printL = W_size