Summary :
1. fix horizontal grain 
    do this
        1.1 storePaperSize / use Case : 1
        1.2 setCalculateLayout(index) / calculation.js:1547
        ** setParallelRollWidth(item) ** set paper grain = box grain
        1.3 checkIsSwitchDisplay(item) / Comment about switch display
        // problem w_range , l_range machine can't fit std. paper

        1.4 setBlockDiecutRate2 / calculation.js:5135
**
* paper_info = ข้อมูลกระดาษ
* layout.selected_layout = layout ที่เลือก
* laySize = layout.selected_layout.layout_size

split
1. $('body').on('change',('.cut_size_std_paper')
2. storePaperSize




**
bug : 
setCutSize(item,is_useMachineSize) // calculation.js:843 


setPaperSize จากไหนได้บ้าง
1. storePaperSize // get std_id > หา ref > setPaper
2. setMachine // หา std > หา ref > setPaper
3. setCutSize tion.js:789 // บัค
4. setCalculateLayout mate.js:3325 // get std_id > หา ref > setPaper



case : กระดาษไม่สามารถลงเครื่องได้
แก้ไข
checkLaysize(index) mate.js:4002 


สรุป : 
checkIsSwitchDisplay = switch lay