รายการที่แก้ไข
function_estimate_calculation:761
old
			var defaultPaperSize = [
				obj.max_size[2],
				obj.max_size[3],
				obj.max_size[0],
				obj.max_size[1],
			]
new


    setPaperSize = 
    item.paperSize = defaultPaperSize
    item.paper_info = {
        cut_off: 32.05
        is_switchDisplay: 0
        paper_align: "short"
        paper_grain: "horizontal"
        parallel_roll_width: "WSize"
        roll_width: 17.25
    }


setComponent1(arr) 
function_estimate_calculation:617
setPackagingSize :830
-----------------
Note :
1. click #calc_layout readyFunction:285

.recalc_layout_bttn : readyFunction:330
>> setCalculateLayout(index,false)

---- save to db ----
laySize_width:item.layout.laySize[2],
laySize_length:item.layout.laySize[3],
--
roll_width : ความกว้างหน้าม้วน paper_info
cut_off : ความยาว paper_info
paperSize_width_in : กว้าง item.paperSize[2]
paperSize_length_in : ยาว item.paperSize[3]
paper_grain : vertical / horizontal
>> vertical ? roll_width = paperSize_length_in
            : paperSize_width_in

-----
** setCalculateLaysize(index,is_useMachineSize)
>> about set layout.selected_layout 
    >> item.layout.laySize
Used : 
1. readyFunction:315 (index,true)
2. readyFunction:1354 (index)
3. estimate.js:1540 (index,true)
4. estimate.js:3248 (index,is_useMachineSize)


** setCalculateLayout(index,is_useMachineSize)
** START >>
*** คำนวณ Layout
* calculation.js:1700
    1.laying = setcalculateLayoutSize(index)
        >> about calc. layout and return best amount any side
            >> w_side คือ ขนาด open size ด้านกว้าง
            >> l_side คือ ขนาด open size ด้านยาว
            switch >> box_template >>
                laying = [{
                >> laying : การวางแนวชิ้นงาน vertical / horizontal
                >> grain_box_type : แนวเกรนกระดาษ vertical / horizontal
                >> layout: [จำนวนงานด้านกว้าง,จำนวนงานด้านยาว]
                >> printing: [ขนาด lay ด้านกว้าง , ขนาด lay ด้านยาว]
                >> num_laying: จำนวนชิ้นทั้งหมดที่วางได้
                >> paper_size: [ขนาดกระดาษด้านกว้าง,ขนาดกระดาษด้านยาว]
                }]

            return {
                laying:laying,
                gripper:gripper,
                color_bar:color_bar,
                paper_edge:paper_edge
            }
    * calculation.js:1701
    2.laying = checkMatchMachineLayout(index,laying,item.component_type.type,item)
        >> checkMaximunNumLayout(laying) // หาว่า lay ไหนได้ชิ้นงานมากสุด
        >> checkMinAreaUsageLayout(correct_laying,num) // หา [w,l] ของ lay ที่ได้ชิ้นงานมากสุด
        >> return layout ที่ได้จำนวนงานมากที่สุด + ใช้พื้นที่น้อยที่สุด

    3.set laying[index].layout_size = [layout + gripper + color_bar + paper_edge]
    4.set item.layout.selectd_layout = best layout
    5.setParallelRollWidth(comp) // set paper_info from best layout
** END setCalculateLayout

-----------

summary :
    setCalculateLayoutSize(index) ใช้เพื่อคำนวณหา layout แต่ละรูปแบบที่จัดวางได้
    checkMinAreaUsageLayout(laying,num_laying) หา layout ที่ได้จำนวนมากที่สุด และใช้พื้นที่น้อยที่สุด
    setMachine(comp,cut_size) 
       1. กำหนด machine cut size และ set std paper
    setPaperSize() กำหนดขนาดกระดาษ grain ของแต่ละ Comp. ลงใน comp.paper_info 
    * checkMatchMachineLayout(index,laying,component_type,comp.) 
        1. ตรวจสอบ Layout ถ้าวางงานได้ ใช้ layout นี้
        2. ถ้าวางงานไม่ได้เลย คำนวณ และคืนค่า lay ใหม่กลับไป
    paper_info / set ได้จาก setPaperSize และ setParallelRollWidth (มาจาก best layout ที่คำนวณได้)
    setCalculateLaysize(index,is_useMachineSize) 
        1. คำนวณ item.layout.laySize[w,l,wmm,lmm]
        2. setCutSize(item,is_useMachineSize)
    setCutSize(item,true/false) // action only when false
        1. เช็คว่าได้ใช้กระดาษตามเครื่องหรือไม่ ? ถ้าไม่ ไป 2
        2. หาด้านสั้น / ยาวของ paper ที่ใช้วาง lay
        3. check ว่า cut size ที่ใช้อยู่ใช้ paper นี้ได้ไหม
        4. ถ้าไม่ได้ หา cut size อื่น และ set item.machine = cut size ใหม่
        5. setPaperSize(comp)
    setDefaultPacking(index) readyFunction.js:316 // คำนวณเกี่ยวกับการแพคส่ง
        1.getPackingSize(comp)
            1.1 packing_size = setCalculatePackingSize(comp) item.packaging_size >> [a,b,c,d]
        2.setKraftwrap(index,(obj from 1))
        3.setCarton(index,"")
        4.setPaperband(index,"")
        5.setPallet(index,"")
    setCalculateUps(index)  readyFunction.js:317
    tbComponent(index) readyFunction.js:318
bug :
    calculation.js:832 >> this.setPaperSiz(item) // ไม่มี arr paperSize และ obj 

Note 23.2.22 22.00 :
imgLayout mate.js:3028 // แสดงผลรูป ไซส์กระดาษ
--
paper_info
    > roll_width  / หน้ากว้างกระดาษ แนวตั้ง
    > cut_off / ความยาวด้านตัด แนวนอน
--
setCalculateSplit(index) lation.js:2958
1. est.setCalculateLayout // mate.js:3243 
    > * storePaperSize(index) // mate.js:104
        // setPaperSize from std_paper_id
        > setPaperSize
    > setCalculatePaperWeight(index)
-----------------------------
todo :
    1. split 
    2. default paper ที่วางงานได้มากสุด


